/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "source.unsplash.com",
      "th.bing.com",
      "res.cloudinary.com",
    ],
  },
};

module.exports = nextConfig;
